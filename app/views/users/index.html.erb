<h2>チーム一覧</h2>
  <div class="row">
    <div class="col-sm-6 col-sm-offset-3">
      <ul class="users">
        <% @users.each do |user| %>
          <li>
            <%= link_to user.username, user %>
          </li>
        <% end %>
      </ul>
　　</div>
　</div>
